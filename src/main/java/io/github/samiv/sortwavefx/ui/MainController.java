package io.github.samiv.sortwavefx.ui;

public class MainController {
    /*
     * Some variable to hold whatever container we will use to hold the bars
     * array (fixed for now, until I do my UI sprint)
     *
     * default constructor
     *
     * Method to conduct a fisher-yates shuffle, or is this blur data and ui, could make
     * fisher-yates a faux SortingAlgorithm, and do it through SortAndVisualTask called by
     * MainController on start of a sort
     *
     * And then sorting logic, for now, it seems fine as the task was to just get it looking nice
     * no need for a start button yet, just a prototype kinda
     */
}
